# Linked Lists

TIL that I could implement linked lists in JavaScript like the following:

```
class LinkedListNode(value) {
    constructor(value) {
        this.value = value;
        this.next = null
    }
}
```
